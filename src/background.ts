/**
 * Browser-command boundary reserved for future explicit user-initiated actions.
 * This scaffold deliberately performs no browser API work.
 */
export function initializeBrowserCommandBoundary(): void {}

initializeBrowserCommandBoundary();
